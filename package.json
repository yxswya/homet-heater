{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.tsx",
    "scan": "bun run video-scanner.ts",
    "start": "bun run server.ts",
    "build": "bun build ./server.ts --compile --target=bun-windows-x64 --outfile mycli"
  },
  "dependencies": {
    "@hono/node-server": "^1.8.0",
    "dotenv": "^17.3.1",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "drizzle-kit": "^0.31.9"
  },
  "type": "module"
}
